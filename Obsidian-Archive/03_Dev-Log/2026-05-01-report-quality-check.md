# 2026-05-01 Report Quality Check

## 실행 결과

요청 날짜 `2026-05-01`은 거래일 데이터가 없어 최신 장마감 데이터인 `2026-04-30`으로 fallback 되었다.

생성 파일:

```text
Obsidian-Archive/Market-Reports/2026/04/2026-04-30-korea-market-report.md
```

## 확인한 것

- `pykrx` 설치 완료
- KRX 엔드포인트는 현재 `LOGOUT` 응답을 반환해 `pykrx` 단독 조회가 실패함
- Naver 모바일 증권 시세 fallback으로 최신 장마감 시세 수집 성공
- ETF/ETN/스팩 제외 필터 적용
- 선별 종목 수: 41개
- 상한가 추정: 9개
- 12% 이상 상승: 27개
- 거래량 1,000만 주 이상: 21개

## 품질 메모

- 시세 표와 종목별 Markdown 구조는 Obsidian에서 쓰기 좋은 형태로 생성됨
- ETF/ETN/스팩 노이즈 제거 후 특징주 리포트로 보기 좋아짐
- 네이버 뉴스 검색 API 키가 아직 없어 기사 링크와 뉴스 요약은 비어 있음
- 뉴스 API 키 입력 후 부각 원인 품질을 다시 점검해야 함

## 다음 작업

- `.env`에 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 입력
- 뉴스가 포함된 리포트 재생성
- 기사 제목/요약 기반 부각 원인 분류 품질 확인
- 필요하면 종목 수 제한 또는 거래대금 기준 정렬을 추가

## 뉴스 API 키 입력 후 재검증

- `.env`에서 네이버 API 키 인식 확인
- 최초 실행 시 `429 Too Many Requests`가 발생해 뉴스 API 호출 간격과 재시도 로직 추가
- `OR`가 포함된 검색어가 네이버 뉴스 API에서 결과 0건을 반환해 검색어 전략 변경
  - 기존: `종목명 특징주 OR 상한가 OR 급등 OR 거래량`
  - 변경: `종목명 특징주`, `종목명 상한가/급등/거래량`, `종목명` 순차 검색
- 종목명이 제목/요약에 포함된 기사를 우선 정렬하도록 관련성 점수 추가
- 부각 원인은 첫 번째 관련 기사 중심으로 추론하도록 개선
- 최종 리포트 기사 링크 수: 202개
- 검색 결과 없음 문구: 0개

## 남은 품질 이슈

- 일부 종목은 단독 기사보다 여러 종목을 묶은 특징주 기사가 먼저 잡힐 수 있음
- 규칙 기반 원인 추론은 빠르지만 완벽하지 않으므로, 추후 LLM 요약 또는 공시 데이터 결합이 필요할 수 있음
